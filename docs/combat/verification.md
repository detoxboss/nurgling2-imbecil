---
doc_id: combat-verification
revision: 1
status: current
last_verified: 2026-07-27
verified_against: "HEAD 9d7404fa0 + uncommitted worktree"
canonical_for:
  - "Traceability matrix: which claims/contracts have actually been tested, how, and with what result"
---

# Verification matrix

Canonical owner of **verification status**. Matching code is not a passing test — every row below distinguishes "the code implements this" (a [code-map.md](code-map.md) claim) from "this was actually run/checked and produced a result" (this file). No automated test harness exists in this repository ([`build.xml`](../../build.xml) has no test target, confirmed by direct inspection), so almost every functional row is `NOT RUN` (no harness exists to run it) or `MANUAL PENDING` (a human must exercise it in-client) — this is a statement of fact, not a placeholder to be filled in casually later.

Statuses used: `PASS`, `FAIL`, `NOT RUN`, `NOT IMPLEMENTED`, `MANUAL PENDING`, `NOT TESTABLE WITH CURRENT HARNESS`.

| ID | Claim/contract | Evidence | Automated test | Manual check | Latest result | Implementation identity |
|---|---|---|---|---|---|---|
| `VER-TRUTH-TABLE` | [BEH-ATK-002](behavior-contract.md#attack-recommendation-and-manual-trigger) exact attack truth table (all 8 cases in the port brief §13 acceptance list) | `EV-SRC-COMBAT-PKG` (`CombatDecisionEngine.chooseAttack`) | None exists | Traced by hand against all 8 listed cases during implementation this session (all-zero→Barrage; no-2IP FC>STING→FullCircle; no-2IP FC==STING>0→FullCircle; no-2IP STING>FC→Barrage; 2+IP STING>FC→Sting; 2+IP tie→FullCircle; 2+IP FC==0,STING>0→Sting; unknown IP never→Sting) | PASS (manual code trace only — not exercised in a live fight) | HEAD 9d7404fa0 + worktree |
| `VER-DEFENSE-TIES` | [BEH-DEF-002](behavior-contract.md#defense-automatic)/[BEH-DEF-003](behavior-contract.md#defense-automatic) tie mapping (red/yellow dedup, green/blue dual-candidate) | `EV-SRC-COMBAT-PKG` (`CombatDecisionEngine.chooseDefence`) | None exists | Traced by hand this session | PASS (manual code trace only) | HEAD 9d7404fa0 + worktree |
| `VER-STATE-001` | [BEH-FAIL-001](behavior-contract.md#fail-closed-handling) — all seven states distinguishable and fail-closed | `EV-SRC-COMBAT-PKG` (`CombatOpeningState`), `EV-GREP-AUDIT` | None exists | Source-read confirms only 5 of 7 states are ever actually produced (`CONFIRMED_ZERO`, `VALID_NOT_FOUND`, `DISPLAY_ABSENT`, `SEARCH_ERROR`, `UNAVAILABLE`); `UNKNOWN` and `STALE` are declared but never constructed | **FAIL (partial)** — fail-closed *behavior* holds for the states that are produced, but the "keep all seven states distinct" requirement is not fully met. See [UNR-002](unresolved.md#unr-002) | HEAD 9d7404fa0 + worktree |
| `VER-ADAPTER-001` | Adapter never silently converts missing/loading data to zero | `EV-SRC-COMBAT-PKG` (`CombatStateAdapter.readOpening`) | None exists | Source re-read this session identified a gap: a `Buff` whose resource name cannot yet be resolved (`resolveResName` returns `null`) is silently skipped in the scan loop, and if no other buff matches, the color falls through to `VALID_NOT_FOUND` (safe zero) rather than `UNKNOWN` — even though the true state is "can't tell yet," not "confirmed not present." | **FAIL (logical gap identified by code inspection)** — plausible defect; real-world frequency (how often `resnm()` actually returns null for an attached `Buff` mid-combat) not observed. See [UNR-002](unresolved.md#unr-002) | HEAD 9d7404fa0 + worktree |
| `VER-IP-GATING` | [BEH-STING-001](behavior-contract.md#attack-recommendation-and-manual-trigger) — unknown/absent/relation-mismatched IP never authorizes Sting | `EV-SRC-COMBAT-PKG` (`CombatDecisionEngine.chooseAttack`, `CombatReactorController.onManualAttackTrigger`) | None exists | Traced by hand: `hasTwoIP` forced `false` whenever `ip.state.isUnsafe()`; manual trigger separately re-checks `ip.state.isUnsafe() || ip.amount < 2` before Sting | PASS (manual code trace only) | HEAD 9d7404fa0 + worktree |
| `VER-RELATION-SWITCH` | [BEH-SCOPE-002](behavior-contract.md#lifecycle-and-scope) — switching target does not leak old relation's IP/openings/pending work | `EV-SRC-COMBAT-PKG` (`CombatReactorController.tick` gob-id revision bump) | None exists | Not exercised in a live fight with a target switch | MANUAL PENDING | HEAD 9d7404fa0 + worktree |
| `VER-CANCEL-001` | [BEH-CANCEL-001](behavior-contract.md#lifecycle-and-scope) — disable/relation-change/combat-end releases any held action | `EV-SRC-COMBAT-PKG` (`CombatReactorController`) | None exists | Source re-read confirms **no such call path exists** — see [code-map.md](code-map.md#relation-change-and-combat-end-cancellation) | **FAIL (confirmed by source inspection)** — see [UNR-005](unresolved.md#unr-005) | HEAD 9d7404fa0 + worktree |
| `VER-DISABLE-001` | Disabling mid-combat stops sending | `EV-SRC-COMBAT-PKG` (`CombatReactorController.tick` early-return on `!reactorEnabled`) | None exists | Not exercised live | MANUAL PENDING | HEAD 9d7404fa0 + worktree |
| `VER-DECK-MISSING` | Missing configured move → `UNAVAILABLE`, no substitution | `EV-SRC-COMBAT-PKG` (`CombatStateAdapter.readActions`, `CombatActionExecutor.send`) | None exists | Traced by hand | PASS (manual code trace only) | HEAD 9d7404fa0 + worktree |
| `VER-COOLDOWN-REJECT` | Action on cooldown is rejected, not sent | `EV-SRC-COMBAT-PKG` (`CombatActionExecutor.send` `readyNow` check) | None exists | Traced by hand | PASS (manual code trace only) | HEAD 9d7404fa0 + worktree |
| `VER-HELD-RELEASE` | Held action is properly released after each send | `EV-SRC-COMBAT-PKG`/`EV-SRC-FIGHTSESS-DIFF` | None exists | Traced by hand: `send()` always pairs `requestAction`+`releaseAction` synchronously | PASS for the synchronous-pair case; **NOT COVERED** for the disable/relation-change-mid-hold case (= VER-CANCEL-001's gap) | HEAD 9d7404fa0 + worktree |
| `VER-MANUAL-001` | [BEH-MANUAL-001](behavior-contract.md#attack-recommendation-and-manual-trigger) manual hotkey behavior | `EV-SRC-COMBAT-PKG` (`CombatReactorController.onManualAttackTrigger`) | None exists | Traced by hand; confirmed no explicit relation-revision equality check (bounded by ~100 ms tick staleness instead) and no retry queue | PASS with documented variance — see [UNR-008](unresolved.md#unr-008) | HEAD 9d7404fa0 + worktree |
| `VER-EXEC-001` | [BEH-EXECCONFIRM-001](behavior-contract.md#execution-confirmation) execution confirmation semantics | `EV-SRC-COMBAT-PKG` (`CombatActionExecutor.Status`) | None exists | Confirmed only `SENT`/`REJECTED` exist; no execution acknowledgement consumed | **NOT IMPLEMENTED** (by design gap, not oversight — see [UNR-006](unresolved.md#unr-006)) | HEAD 9d7404fa0 + worktree |
| `VER-DIFF-001` | Snapshot diffing (`sameDecisionState`) actually gates recomputation | `EV-SRC-COMBAT-PKG`, `EV-GREP-AUDIT` | None exists | Grep confirms zero call sites for `sameDecisionState` anywhere in the codebase | **NOT IMPLEMENTED** (dead code — the method exists but nothing calls it) — see [UNR-007](unresolved.md#unr-007) | HEAD 9d7404fa0 + worktree |
| `VER-SETTINGS-001` | Settings persistence (enable checkbox round-trips through `NConfig`) | `EV-SRC-SETTINGS`, `EV-SRC-WIRING-DIFFS` | None exists | Traced by hand: `load()`/`save()` symmetric with `AutoLogoutSettings`'s established pattern | PASS (manual code trace only) — not exercised via the actual Settings UI in a running client | HEAD 9d7404fa0 + worktree |
| `VER-HOTKEY-001` | Global hotkey collision-safety (`E` binding) | `EV-SRC-WIDGET`, `EV-SRC-WIRING-DIFFS` | None exists | Grepped `VK_E` across `src/`: only this binding and unrelated low-level toolkit key-code tables (`AWTToolkit.java`, `NEWTContext.java`) reference it — no other gameplay keybind collision found | PASS (grep-based, this session) | HEAD 9d7404fa0 + worktree |
| `VER-KEYBOARD-REGRESSION` | Normal (non-reactor) keyboard combat input unchanged after the `Fightsess` extract-method refactor | `EV-SRC-FIGHTSESS-DIFF` | None exists | Diff re-read byte-for-byte this session: `globtype`'s key-hit branch and `keyup` now call `requestUse`/`releaseHeld`, whose bodies are verified identical to the original inline logic (same order of held-release, `Maptest` hit/no-hit, `holdgrab` handling) | PASS (manual diff re-verification only) | HEAD 9d7404fa0 + worktree |
| `VER-CRASH-FIX` | The specific multi-session crash the user hit (`NUtils.getGameUI()` returning null / resolving the wrong session) does not recur | `EV-CRASH-REPORT`, `EV-SRC-WIDGET` | None exists | Root cause identified (`NCombatReactor` used the global session lookup instead of `getparent(GameUI.class)`) and fixed this session; a second latent bug (executor never rebuilt on `Fightsess` identity change) found and fixed in the same pass | **MANUAL PENDING — user has not yet reproduced the original tab-switch-mid-fight scenario against the fixed code** | HEAD 9d7404fa0 + worktree |
| `VER-TRIP-SWITCH` | [BEH-UI-002](behavior-contract.md#diagnosticsui-approved-minimum) safety trip switch actually stops sending after 3 failures and shows the warning | `EV-SRC-WIDGET` (`NCombatReactor.handleFailure`, `effectiveEnabled`) | None exists | Traced by hand: `effectiveEnabled()` ANDs in `!tripped`; both `tick` and `handleGlobalKey` route through it | PASS (manual code trace only) — not exercised by deliberately forcing 3 failures in a live client | HEAD 9d7404fa0 + worktree |
| `VER-BUILD` | Project compiles | `EV-BUILD-20260727` | `ant jar` (the repository's only documented build/verification command per [CLAUDE.md](../../CLAUDE.md)) | — | **PASS** — exit code 0, `BUILD SUCCESSFUL`, `build/hafen.jar` produced, 2026-07-27 12:23:23 -06:00 | HEAD 9d7404fa0 + worktree |

## Explicitly not claimed

No row above claims an actual in-game combat session was run by the assistant. Per task constraints, in-game manual verification is the user's to perform and report; every `MANUAL PENDING` row above is waiting on that, not on further code changes.

## See also

- [unresolved.md](unresolved.md) — the open items each `FAIL`/`NOT IMPLEMENTED` row links to.
- [behavior-contract.md](behavior-contract.md) — the contract each row verifies (or fails to).
- [evidence.md](evidence.md) — full detail on each evidence ID cited above.

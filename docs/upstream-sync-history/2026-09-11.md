# Upstream sync — 2026-09-11

**Status:** merge commit exists and is pushed; PR open, **not yet merged to `master`**; runtime
verification **still pending**. Do not treat this cycle as complete until those two facts change —
this entry will be updated in place once they do.

## Refs

| | |
|---|---|
| Pre-sync `master` | `f8b8bb9c706bd3d1cdc0b8b6c5dbc8369ee99db6` |
| Previously integrated upstream (merge-base) | `d8e0ad28eefd048d280bf543d00dd2f0aa2ede8b` (unchanged from the 2026-09-04 cycle — no upstream merge landed between that cycle and this one) |
| Pinned upstream target | `b767e561791d85c7249ae24bd3dcda4f5c464e5d` |
| Merge commit | `a4fcab0040108b72569a99fbca667e0050a6bffb` |
| Safety tag | `pre-upstream-sync-2026-09-11-f8b8bb9c7` → `f8b8bb9c706bd3d1cdc0b8b6c5dbc8369ee99db6` |
| Sync branch | `sync/upstream-2026-09-11` |
| PR | (recorded below once created) — **open, not merged** |

## What was integrated

216 new upstream commits, dominated by one mega-branch merge:

- **Forager settings refactor** (~150 commits, 2026-08-27 → 2026-09-09, upstream PR #348): unifies
  Forager's Actions/Routes/Guarding/Maintain into one Presets selector, adds an embedded 3D-map route
  editor (cliff/exclusion-zone painting), a composable Trigger+Outcome "Guarding" safety-watchdog
  architecture, milestone travel recording and waypoint steps (including GateBot), a settings-search
  feature (upstream PR #347), and numerous correctness fixes (ChunkNav races, an `NConfig.needUpdate()`
  session-save bug, a Ring Settings persistence bug, several NPEs).
- **feat/mine-helper** (PR #351) — new `MasterMiner` bot.
- **feat/quick-barrage-spammer** (PR #355) — new `QuickBarrageBot`.
- **feat/irrlight-bot, feat/animal-category, feat/watermelon-radish-white-onion, fix/honey-wax-bot,
  feat/cave-gallery-stop, feat/kiln-and-smelter-helpers** — new bots/icons, additive, no shared-file
  overlap with the fork.
- **feat/quest-helper-map-overlay** (PR #352) — new quest-objective UI widget family, no overlap.
- **feat/fight-window-filters, feat/fill-direction-for-areas** — isolated additions, no overlap.
- **fix/some-npe-fixes** (PR #346) — `NEquipory` null-check, `PathFinder` collision-box fallback, misc.

327 files touched upstream in total; the great majority is Forager-internal or new-bot-icon resource
files with no fork interaction.

## Merge result

Exactly 2 textual conflicts — predicted correctly by the Phase-1 disposable trial merge and reproduced
identically by the real Phase-2 staged merge:

- **`src/nurgling/actions/bots/registry/BotRegistry.java`** — two independent-addition-at-same-anchor
  collisions: fork's `lpassistantbot` vs. upstream's `masterminer`, and fork's `combatreactortool` vs.
  upstream's `quickbarrage`. Resolved by keeping both sides at each site (append, not replace). Verified
  post-resolution: all four ids present exactly once, no duplicates, no import changes needed (both new
  upstream classes already covered by the existing `nurgling.actions.bots.*` wildcard import).
- **`src/nurgling/widgets/NSettingsWindow.java`** — fork's `CombatReactorSettings` menu entry vs.
  upstream's two new entries (`ForagerSettingsPanel`, `MiningMasterySettings`) at the same insertion
  point. Resolved by keeping all three (both new upstream panel classes already covered by the existing
  `nurgling.widgets.nsettings.*` wildcard import).

15 shared-edit files identified and manually reviewed (all auto-merged with zero textual conflict):
`NConfig.java`, `messages.properties`, `messages_ru.properties`, `QoL.java`, `NUtils.java`, `NCore.java`,
`NGameUI.java`, `NMiniMap.java`, `NGItem.java`, `NFightsess.java`, `LpExplorer.java`, `NMapView.java`,
`NBotsMenu.java`, plus the two conflict files above.

- **`NConfig.java`** — `Key` enum: 268 entries pre-merge on both the fork side and the upstream-target
  side (measured independently), 273 after merge; exact identifier-set comparison confirmed zero drops
  on either side and zero duplicates introduced. `autoDrinkThreshold` and its `75` default both present.
  The unexpectedly large `-64`-line fork-side delta noted in Phase 1 was traced to a pre-existing fork
  cleanup (removal of a superseded `isExploredUpd`/debounce mechanism now handled by
  `ExploredArea.saveIfDue()`) that predates this cycle entirely — confirmed unrelated and harmless, not
  something this merge did.
- **`messages.properties` / `messages_ru.properties`** — exactly one `qol.auto_drink_threshold` in each
  after merge; no new duplicate keys introduced by this merge.
- Every other shared file's fork-side addition (see "Fork invariants" below) verified present and intact
  post-merge by direct inspection of the merged content, not inferred from a clean auto-merge alone.

Staged file set: 327 files (`git diff --cached --stat`), matching the raw upstream-delta diffstat
(327 files, 20,532 insertions, 1,363 deletions) exactly — confirms the staged merge is complete and
faithful to what Phase 1 analyzed.

## Fork invariants — confirmed untouched

Every seam in `docs/fork-customization-ledger.md` re-verified directly on the merged tree (not assumed
from a clean textual merge):

- **Combat Reactor protocol hooks** (`src/haven/NFightsess.java`) — `requestUse`/`releaseHeld`/
  `requestAction`/`releaseAction` all present.
- **Owning-session `NGameUI` teardown** (`NCore.java`, `NGameUI.java`) — `ui.core.dispose()` call and its
  explanatory comment intact.
- **Owning-session `NMiniMap`/`GameUI` resolution** (`NMiniMap.java`) — the load-bearing
  `NGameUI gui = (this.ui != null) ? this.ui.gui : null;` line inside `tick()`, with its
  wrong-session-when-backgrounded comment, survived verbatim alongside upstream's large new
  waypoint-overlay addition in the same file.
- **Passive tableware-breakage food-take guard** (`NGItem.java`) — `wdgmsg("take")` gate on
  `autoSaveTableware` + `isFoodTakeBlockedByWornTableware()` intact.
- **Fork configuration keys** (`NConfig.java`) — see above.
- **Fork `BotRegistry` registrations** (`BotRegistry.java`, `NBotsMenu.java`) — see conflict resolution
  above; `NBotsMenu.java` has no hardcoded bot-id references (categories populate dynamically from
  `BotRegistry`), nothing to reconcile there.

## Auto-drink customization — confirmed preserved

Auto-drink (merged to `master` via PR #4, 2026-09-08 through 2026-09-11, immediately prior to this sync
cycle) had **zero upstream changes this cycle** — `git diff` of `src/nurgling/actions/AutoDrink.java`
against pre-sync `master` is empty. Re-verified directly on the real merge commit:

- `NConfig.Key.autoDrinkThreshold` enum entry and its `conf.put(..., 75)` default.
- `QoL.java`'s `autoDrinkThresholdEntry` field, layout, load-with-clamp, save-with-clamp.
- `NUtils.getStamina()`'s `gui == null` guard.
- `AutoDrink.java` itself: `FULL_STAMINA`-first guard ordering, the `active` latch, `getWater()`-based
  water detection, no `waitBot` reference, no `0.51` literal anywhere.

No restoration risk existed this cycle since upstream never touched the feature; recorded here per the
ledger's own Auto-drink entry for continuity into future cycles.

## Build / test

- Pre-sync baseline (on `master` @ `f8b8bb9c706bd3d1cdc0b8b6c5dbc8369ee99db6`, before touching anything):
  `ant test` 21/21 pass, `ant jar` succeeds.
- Phase-2 staged-merge validation (on the uncommitted merge): `ant test` 21/21 pass, `ant jar` succeeds,
  `ant bin` succeeds.
- Phase-3 post-commit rebuild (the only one with correct `buildinfo` attribution): `ant bin` succeeds;
  embedded `buildinfo.git-rev` = `a4fcab0040108b72569a99fbca667e0050a6bffb`, matching the merge commit
  exactly.
- `git diff --check` on the staged merge: zero conflict-marker errors. Flagged only pre-existing trailing
  whitespace inherited verbatim from upstream's own new files (`MasterMiner.java`, `MasterMinerWnd.java`,
  `MiningMasterySettings.java`, `NMasterMinerMarkingConfig.java`, `NMasterMinerProp.java`) — accepted
  as-is per the low-conflict policy (don't reformat a file the fork doesn't own for style alone).

## Still pending on this cycle

- **PR merge to `master`** — not yet done; awaiting review.
- **Targeted in-client runtime verification** — not yet performed. Scope (per the delta actually
  touched): Nurgling Settings window open + settings search, Combat Reactor settings entry, Auto-drink
  setting/threshold/behavior, LP Assistant accessibility, bot menu with all fork bots plus the two new
  upstream bots (`MasterMiner`, `QuickBarrageBot`) registered, the new Forager settings panel, the new
  Mining Mastery settings panel, and the minimap's new waypoint overlay (including the owning-session
  invariant under a backgrounded multi-session tab). Full checklist tracked in the PR description.
